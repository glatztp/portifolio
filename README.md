# portifolio

Template: next

## Comandos

```bash
pnpm install
pnpm run dev
```


## Variáveis de Ambiente

Copie o arquivo `.env.example` para `.env` e ajuste as variáveis conforme necessário:

```bash
cp .env.example .env
```
